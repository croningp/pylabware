Utils
=====

OpenAPI parser
--------------

The parser provided in :file:`utils/openapi_parser.py` provides the basic minimal
functionality to convert OpenAPI specifications in JSON/YAML format into the
PyLabware compatible dictionary-based command definitions.

The usage instructions can be found by running the parser without arguments::

    python openapi_parser.py
